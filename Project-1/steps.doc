1. Signup {All validation, Password is in # formate in data base} 
2. Login {All validation}
3. Home Page {If quantity is 0 then out of stock}
4. Cart.php {User can update quantity}
5. Check out { Details }
6. Process.php { Order Processing and save all info in DB}
         
What left ? 

- Need to put validation in Process.php form it should not allow null value.