<?php

//link connection file 

require_once("config.php"); //PHP will check if the file has already been included, and if so, not include (require) it again

// Define all variables 

$username = $password = $confirm_password =$err_username = $err_password = $err_confirm_password= ' ';



?>
